<apex:component>
    <apex:facet name="header"/>
    <apex:facet name="body"/>

    <div class="box">
        <div class="header">
            <apex:insert facetName="header"/>
        </div>
        <div class="body">
            <apex:insert facetName="body"/>
        </div>
    </div>

    <!-- This is a suppressed warning NOPMD -->
    <aura:attribute name="value" type="String"/>
    <!-- nopmd -->
</apex:component>